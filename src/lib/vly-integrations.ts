// VLY Integrations — placeholder.
//
// Nothing imports this module; it exists only so imports of
// "@/lib/vly-integrations" never break. The app talks to Vly through
// `@vly-ai/integrations`' vite plugin (see vite.config.ts).

export {};
